# Rust
/target

# Node
node_modules/
*.log

# wasm-pack output (rebuilt from source; pkg/ ships via "files" on
# npm publish, not in git — CI and contributors run `npm run build`)
/pkg

# test scratch
test/work/

# browser-test debris (failure screenshots, run state, attachments)
test/__screenshots__/
test-results/
.vitest-attachments/
